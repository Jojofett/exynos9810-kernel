name: Build Kernel

on:
  push:
    branches: [ main, master, duhan-(4.9.337) ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    # 1. Checkout WITHOUT submodules
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        submodules: false   # <-- important: do not auto-fetch submodules

    # 2. Manually handle submodules (fix missing URL, then update)
    - name: Initialize and fix submodules
      run: |
        # If .gitmodules references KernelSU-Next but has no URL, add a placeholder.
        # REPLACE THE URL BELOW WITH THE CORRECT ONE if you know it.
        if grep -q "KernelSU-Next" .gitmodules 2>/dev/null; then
          if ! git config --file .gitmodules submodule.KernelSU-Next.url > /dev/null; then
            echo "Adding missing URL for KernelSU-Next submodule"
            git config --file .gitmodules submodule.KernelSU-Next.url https://github.com/Jojofett/KernelSU-Next.git
          fi
        fi

        # Sync and update submodules; if it fails, remove the broken submodule.
        git submodule sync --recursive
        git submodule update --init --recursive --force || {
          echo "Failed to update submodules. Removing KernelSU-Next directory and its .gitmodules entry."
          rm -rf KernelSU-Next
          git config --file .gitmodules --remove-section submodule.KernelSU-Next 2>/dev/null || true
          git add .gitmodules
          git commit -m "Remove broken KernelSU-Next submodule" || true
        }

    # ------------------------------------------------------------------
    # Rest of the build (dependencies, uH, RKP, linker, etc.)
    # ------------------------------------------------------------------
    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          bc bison build-essential ccache curl flex g++-multilib gcc-multilib \
          git gnupg gperf imagemagick lib32ncurses5-dev lib32readline-dev \
          lib32z1-dev liblz4-tool libncurses5-dev libsdl1.2-dev libssl-dev \
          libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools \
          xsltproc zip zlib1g-dev unzip python3 python3-pip

    - name: Install cross-compilers
      run: |
        sudo apt install -y gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu
        sudo apt install -y gcc-arm-none-eabi binutils-arm-none-eabi   # for uH (if needed)

    - name: Set up ccache
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: ${{ runner.os }}-kernel

    # Build uH firmware if source exists
    - name: Build uh.elf
      run: |
        UH_SOURCE_DIR="init/uh"   # adjust if different
        if [ -f "$UH_SOURCE_DIR/Makefile" ]; then
          echo "Building uH firmware from $UH_SOURCE_DIR"
          make -C "$UH_SOURCE_DIR" CROSS_COMPILE=arm-none-eabi- CC=arm-none-eabi-gcc
          UH_ELF=$(find "$UH_SOURCE_DIR" -name "uh.elf" | head -1)
          if [ -n "$UH_ELF" ]; then
            cp "$UH_ELF" init/
            echo "uh.elf copied to init/"
          else
            echo "ERROR: uh.elf not generated. Check uH build."
            exit 1
          fi
        else
          echo "No uH source found at $UH_SOURCE_DIR. If uh.elf is required, ensure it is prebuilt."
          if [ ! -f "init/uh.elf" ] && grep -q 'incbin.*uh.elf' init/_uh.S 2>/dev/null; then
            echo "WARNING: init/uh.elf is missing. Creating a dummy file to satisfy the build."
            touch init/uh.elf
          fi
        fi

    # Set kernel build environment
    - name: Set build environment
      run: |
        echo "ARCH=arm64" >> $GITHUB_ENV
        echo "CROSS_COMPILE=aarch64-linux-gnu-" >> $GITHUB_ENV
        # Force GNU BFD linker (fixes vDSO error with ld.lld)
        echo "LD=aarch64-linux-gnu-ld.bfd" >> $GITHUB_ENV
        echo "/usr/lib/ccache" >> $GITHUB_PATH

    - name: Clean build directory
      run: make clean && make mrproper

    # Generate defconfig – CHANGE THIS to your actual defconfig name
    - name: Generate defconfig
      run: make exynos9810_defconfig   # e.g., a71_defconfig, universal9810_defconfig

    # Disable RKP (fixes rkp_ro_alloc_phys errors)
    - name: Disable RKP
      run: |
        scripts/config --file .config --undefine CONFIG_RKP
        scripts/config --file .config --undefine CONFIG_SAMSUNG_RKP
        scripts/config --file .config --set-val CONFIG_RKP n
        make olddefconfig

    # Build kernel image – CHANGE if your kernel outputs zImage or Image.gz
    - name: Build kernel
      run: |
        make -j$(nproc) Image

    # Package kernel
    - name: Check build artifacts
      run: |
        mkdir -p Apollo/Product
        if [ -f "arch/arm64/boot/Image" ]; then
          cp arch/arm64/boot/Image Apollo/Product/kernel.img
        elif [ -f "arch/arm64/boot/Image.gz" ]; then
          cp arch/arm64/boot/Image.gz Apollo/Product/kernel.img
        elif [ -f "arch/arm64/boot/zImage" ]; then
          cp arch/arm64/boot/zImage Apollo/Product/kernel.img
        else
          echo "ERROR: No kernel image found!"
          find . -name "*.img" -o -name "Image*" -o -name "zImage" | head -20
          exit 1
        fi

    - name: Package kernel
      run: |
        cd Apollo/Product
        zip_name="DS-ACK-Kernel-$(date +%Y%m%d-%H%M%S).zip"
        zip -q "$zip_name" kernel.img
        echo "Created $zip_name"
        cd ../..

    - name: Upload kernel image
      uses: actions/upload-artifact@v4
      with:
        name: kernel-image
        path: Apollo/Product/*.zip

    - name: Upload build logs (if any)
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: Build-Logs
        path: |
          build.log
          config.log
          scripts/config.log
